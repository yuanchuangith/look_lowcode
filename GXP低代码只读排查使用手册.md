# GXP 低代码只读排查使用手册

适用插件：`gxp-lowcode-readonly`  
适用 Skill：`gxp-lowcode-debug`  
文档对应版本：`0.3.0+codex.20260826050514`

## 1. 用途

本 Skill 用于只读排查 GXP 低代码的页面、动作、草稿/发布副本、画布节点、字段映射、组件过滤、生成 C# 和业务数据证据。

适合以下问题：

- 通过页面或流程中文名称反查表单动作。
- 公共动作、表单动作的 code、RefId 或显示名称定位。
- 试卷下拉可以选择全部、没有可选值或值被清空。
- `DataFilter` 被初始化、默认值或后续字段事件覆盖。
- 草稿已经保存但运行结果没有变化。
- 发布后复核草稿与发布副本是否同步。
- 动态 C# 异常、字段映射或画布节点定位。
- 岗位培训、周期培训等跨动作字段生命周期排查。
- 低代码配置正确但组件运行异常，需要按 MCP 命中只读查看前端组件实现。
- API、Controller、Service 或后端异常栈已有精确锚点，需要按需查看后端源码。

不适用于：

- 修改业务数据、数据库结构或 ActionDesign JSON。
- 保存草稿、发布动作或修改历史版本。
- 常规功能开发和纯前端组件样式问题。
- 绕过审批执行任何数据库写入。

## 2. 快速调用

推荐直接提供当前现象、期望规则和无匹配行为：

```text
[$gxp-lowcode-readonly:gxp-lowcode-debug]

当前现象：<用户实际看到的结果>
期望规则：<允许选择或写入的值>
无匹配时行为：<为空且不可选择 / 允许全部 / 尚未确认>

涉及页面或流程：<页面名称>
动作 code/RefId：<已知时填写>
组件或字段：<已知时填写>
发生时间和记录 ID：<排查运行样本时填写>

请只读排查，不修改数据、设计、草稿或发布状态。
```

如果信息不完整，也可以直接粘贴用户原话、动作编号或完整异常栈。Skill 会先定位候选，但在三项业务规则未确认前只报告当前配置，不判断配置正确或错误。

## 3. 四项核心原则

### 3.1 先锁定业务规则极性

“现在可以选择全部”可能是故障现象，也可能是业务期望，必须先区分：

```text
当前现象：用户实际看到什么
期望规则：哪些值允许被选择
无匹配时行为：为空且不可选择 / 允许全部 / 尚未确认
```

用户纠正规则后，只重算结论，不重复搜索已经确认的页面、动作和节点。

### 3.2 复用会话锚点

已经确认的 action code、RefId、页面 Route、分组、组件 key 和业务不变量都属于会话锚点。除非动作身份或设计版本可能变化，否则后续追问应直接复用。

### 3.3 分两阶段读取

第一阶段只定位页面、动作、分组、节点和紧凑 `facts`：

```text
include_params=false
include_generated_csharp=false
```

第二阶段必须先缩小到分组、节点 Key 或不超过 10 个节点的画布范围，才读取完整 `paramsValue`。只有异常栈给出精确 C# 行，或节点事实仍不足时，才读取生成 C#。

### 3.4 区分草稿和发布

- 草稿副本可编辑，保存草稿不影响运行。
- 发布副本是运行副本，只有发布才更新运行配置。
- “已同步”只表示当前草稿与发布副本内容相同，不表示逻辑正确或已经运行验证。
- 历史发布是过去运行过的快照；排查历史异常时不能把当前草稿当作运行证据。

每次版本复核都应明确输出：

```text
发布同步状态：已同步 / 存在未发布草稿改动 / 当前副本缺失
运行副本：发布副本
草稿修改是否影响运行：否
```

### 3.5 先命中，再按需查看源码

四个诊断接口会返回紧凑 `source_hints`。只有低代码证据不足且其中出现明确层级和关键词时才读源码：

- 组件 type/key/model key → 前端 `G:\hoyi\updateComponents\gxp2.components`。
- API、Controller、Service、方法或真实后端栈 → 后端 `G:\hoyi\updateWeb\gxp2.web`。
- 同时命中请求构造与接收字段 → 先前端、再后端。
- 只有字段、表名、`CallAction` 或动态类名 → 不扫源码，继续低代码链路。
- 没有源码锚点但缺真实交互 → 升级浏览器运行取证。

受限脚本 `skills/gxp-lowcode-debug/scripts/search_source_evidence.py` 只使用 `rg` 搜索固定仓库，不构建、不修改、不自动扩大目录。最多返回 20 个文件、前 5 个上下文，JSON 不超过 32 KB。

## 4. 标准排查流程

1. 将用户原始输入完整交给 `diagnose_codex_input`，异常栈不要预先改写。
2. 有页面或流程名称时，先用 `search_pages` 定位 Route、Id 或 OutId，再用 `list_page_actions` 枚举页面动作。
3. 有动作 code/RefId 时使用 `resolve_action`；只有中文业务名称或精确标识未命中时使用 `search_actions`。
4. 用 `get_design_versions` 读取当前草稿和发布副本；只有历史异常才加载发布历史。
5. 用 `inspect_action` 做第一阶段紧凑定位，缩小范围后再读取完整参数。
6. 下拉、过滤、值被清空或覆盖问题必须使用 `inspect_component_filters`，审计同一组件的全部 `DataFilter` 写入点。
7. 需要业务数据证据时，先 `describe_table`，再用 `get_records` 进行索引等值、小列集、小数量查询。
8. 沿 `CallAction`、`CallPublicAction` 追踪到最终数据动作、显式返回或首个异常点，再形成结论和画布修改方案。

## 5. 15 个 MCP 工具

| 分类 | 工具 | 用途 |
| --- | --- | --- |
| 连接 | `connection_status` | 检查外部配置和只读数据库连接。 |
| 输入路由 | `diagnose_codex_input` | 解析自然语言、动作标识、页面语义或异常栈并推荐下一步。 |
| 页面定位 | `search_pages` | 按 Route、Id、OutId、中文名称或受限中文相似度查找页面。 |
| 页面动作 | `list_page_actions` | 枚举一个精确页面的全部有效表单动作。 |
| 动作搜索 | `search_actions` | 按中文名称、code 或 RefId 搜索公共/表单动作。 |
| 动作解析 | `resolve_action` | 精确解析动作 code/RefId，必要时回退名称搜索。 |
| 版本读取 | `get_design_versions` | 读取当前草稿、当前发布和可选历史发布快照。 |
| 版本比较 | `compare_designs` | 判断草稿与发布副本是否存在未发布差异。 |
| 画布检查 | `inspect_action` | 返回有界节点事实、必要参数和按需生成 C# 证据。 |
| 过滤审计 | `inspect_component_filters` | 汇总同一组件跨分组、跨阶段的全部过滤写入点。 |
| 条件验证 | `evaluate_node_predicate` | 使用给定记录验证一个画布节点条件中的字面量部分。 |
| 异常追踪 | `trace_dynamic_exception` | 将动态 C# 异常行映射到发布时间、动作和候选画布节点。 |
| 表结构 | `describe_table` | 在读取业务表前返回字段和索引。 |
| 业务记录 | `get_records` | 使用结构化、窄条件和硬限制读取业务记录。 |
| 受控 SQL | `readonly_sql` | 结构化工具无法表达时执行一条 AST 校验的只读 SQL。 |

### `inspect_action` 限流规则

- 默认 `include_generated_csharp=false`。
- 传入 `terms` 不代表允许返回生成 C#。
- 默认 `max_nodes=20`，超限时返回截断信息和缩小范围建议。
- `include_params=true` 命中超过 10 个节点时只返回紧凑预览，不返回大块参数。
- 生成 C# 搜索结果最多返回 20 处。
- 传入精确 `csharp_line` 时仍可返回该行上下文。

## 6. 组件过滤回归用例

> 本节只演示如何表达“当前现象、期望规则、无匹配行为”，不固化任何页面、动作、组件或字段组合。具体约束必须从当前对话和当前发布设计中提取。

### 推荐提问

```text
[$gxp-lowcode-readonly:gxp-lowcode-debug]

当前现象：<哪些页面或交互中，组件实际显示了什么>
期望规则：<只允许选择满足哪些关联字段、状态和范围条件的数据>
无匹配时行为：<空集合 / 允许全部 / 保留原值 / 其他已确认行为>

请先定位当前页面对应的表单动作和目标组件，再检查该组件的全部 DataFilter 写入点，确认初始化、默认值、字段变更和事件绑定是否存在后写入覆盖。如果期望规则未说清，只报当前配置，不判定正确性。只读排查，不修改或发布。
```

### 定位范围

- 只有页面或流程名称时，先用 `search_pages` 定位 Route/Id/OutId，再用 `list_page_actions` 枚举表单动作。
- 按当前命中结果记录 action code、RefId、分组和组件 key；这些只是本次会话锚点，不是其他问题的默认值。
- 最终结论必须来自当前发布副本中该组件的全部过滤写入点，不能只看单个初始化节点。

### 必须回答的验收问题

- 用户点名的每个页面是否都定位到正确 Route 和表单动作。
- 每个动作的草稿与发布副本是否同步，当前运行的是哪个发布设计。
- 同一目标组件共有多少个过滤写入点，是否覆盖全部分组和执行阶段。
- 父级 IF/Else 条件是否与用户实际操作路径一致。
- 哪个可达的后写入节点最终决定候选范围。
- 最终过滤是否同时满足用户本次确认的全部关联字段、运算符、状态和范围条件。
- 找不到对应关系时是否得到空集合，还是因过滤缺失退化为全部候选。
- 当前结论属于静态发布设计验证，还是已经完成真实页面运行验收。

## 7. 其他常用提问模板

### 保存草稿后复核

```text
[$gxp-lowcode-readonly:gxp-lowcode-debug]
动作：<code 或 RefId>
我已经保存草稿。请只读比较草稿和发布副本，并检查目标分组、节点的完整参数；不要发布。
```

### 发布后复核

```text
[$gxp-lowcode-readonly:gxp-lowcode-debug]
动作：<code 或 RefId>
我已经发布。请重读当前发布副本，核对目标节点和草稿/发布同步状态，并区分静态验证与运行验证。
```

### 动态异常追踪

```text
[$gxp-lowcode-readonly:gxp-lowcode-debug]
异常发生时间：<时间>
完整异常栈：
<原样粘贴异常栈>

请按异常时间选择当时运行的发布快照，定位生成 C# 行和候选画布节点。
```

## 8. 诊断输出要求

诊断回答第一行直接给结论：

```text
问题1：<一句话结论>
```

每个位置都应完整给出：

```text
位置1
动作：<code>／<显示名称>
RefId：<RefId>
分组：<显示名称>／<group_key>
画布行／索引：<1-based 行>／<0-based 索引>
节点：<node_key>／<显示名称>
类型：<节点类型>
上一节点：<显示名称>
下一节点：<显示名称>
当前：<完整条件、映射或过滤事实>
生成 C#：<精确行 / 候选行 / 未读取>
```

随后按证据补充失败机制、只读数据、下游约束、画布修改、生命周期保留项和验证状态。没有证据的段落应省略，不写“同上”。

修改步骤不能只使用内部 Key 指路，必须同时提供动作显示名称、分组显示名称、画布行、节点显示名称和上下相邻节点。

## 9. 证据和验收边界

- 当前发布设计证据：证明线上配置当前是什么。
- 草稿/发布哈希同步：只证明两个副本相同。
- 只读业务样本：用于验证实际记录值和可能命中的写入路径。
- 页面运行证据：用于确认真实交互、请求、响应和最终行为。
- 静态检查通过不等于部署成功，也不等于真实业务运行验收通过。

只有 ActionDesign、生成 C#、只读数据和历史快照仍无法解释问题时，才升级到已授权的 `js-reverse-extension` 页面取证。不得点击创建、保存、提交、推送或发布类控件。

## 10. 强制安全边界

- 只允许 `SELECT`、`SHOW`、`DESCRIBE`、`EXPLAIN`。
- 不读取、询问、输出或转述数据库密码与连接凭据。
- 不直接调用 `mysql.exe`，不自行拼接数据库连接参数。
- 不修改业务数据、表结构、动作 JSON、草稿、发布状态或历史记录。
- `readonly_sql` 只用于结构化工具不能表达的窄查询，并必须说明原因。
- 工具故障只能报告为工具故障，不能据此认定业务 Bug。

## 11. 项目文件

- 插件部署和接口说明：`README.md`
- Skill 主规则：`skills/gxp-lowcode-debug/SKILL.md`
- 平台和版本定位：`skills/gxp-lowcode-debug/references/platform-map.md`
- 诊断输出契约：`skills/gxp-lowcode-debug/references/report-contract.md`
- 组件过滤审计：`skills/gxp-lowcode-debug/references/component-filter-audit.md`
- 页面运行取证：`skills/gxp-lowcode-debug/references/runtime-browser-evidence.md`
- MCP 入口：`mcp/server.py`
- HTTP MCP 入口：`mcp/http_server.py`

本文档面向 Skill 使用者；部署、数据库配置和 systemd 服务说明以项目根目录 `README.md` 为准。
